rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Warning: This is insecure. Everyone can read and write to all data.
      allow read, write: if true;
    }
  }
}
